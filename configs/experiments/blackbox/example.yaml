schema_version: 1
experiment_track: blackbox_memory_poisoning

dataset:
  name: personamem_v2
  revision: replace-with-immutable-revision
  subset: blackbox_battery_v1

condition:
  mechanism: telemem
  mechanism_version: 1.10.0
  write_policy: native_selective
  mechanism_options:
    infer: true

models:
  writer: {provider: ollama, model: "mistral:replace-with-pinned-id"}
  responder: {provider: ollama, model: "mistral:replace-with-pinned-id"}
  embedding: {provider: ollama, model: "replace-with-pinned-embedding-id"}

retrieval:
  top_k: 5
  threshold: null
  context_token_budget: 2048

attack:
  knowledge: black_box
  delivery: conversation
  battery: pmv2_blackbox_battery_v1
  attack_id: bb-pi-001

isolation:
  run_id: example-do-not-execute
  config_ref: planning/config.md

replication:
  seed: 20260831
  response_replicates: 1
